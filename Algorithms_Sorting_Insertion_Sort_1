def insertionSort1(n, arr):
    nuni= arr[-1]
    counting = 1
    for i in arr: 
        if i > nuni:
            if arr[(n-1)-counting] > nuni:
                arr[n-counting] = arr[(n-1)-counting]
                counting += 1
            print(arr)
    arr[(n)-counting] = nuni
    print(arr)
